// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id           Int        @id @default(autoincrement())
  name         String
  email        String     @unique
  password     String?
  gender       userGender @default(none)
  role         userRole   @default(admin)
  placeOfBirth String?
  dateOfBirth  DateTime?
  status       userStatus @default(active)
  image        String?
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt

  studyGroups Group[]
}

enum userGender {
  none
  male
  female
}

enum userRole {
  admin
  student
}

enum userStatus {
  active
  inactive
  deleted
}

model Group {
  id           Int      @id @default(autoincrement())
  name         String
  leader       String
  subject      String
  scheduleTime DateTime
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  students User[]
}
